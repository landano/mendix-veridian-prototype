import * as variables from "../../../theme/native/custom-variables";

export const cardLandano = {
    container: {
        backgroundColor: "rgba(255, 255, 255, 0.6)",
        padding: 20,
        borderRadius: 10,
        shadowColor: "#000",                          // Black shadow color
        shadowOffset: { width: 0, height: 1 },        // Small vertical offset
        shadowOpacity: 0.1,                           // Light shadow (10% opacity)
        shadowRadius: 2                              // Small blur radius (2dp)
    }
}

